name: Release (legacy)

# Deprecated: releases are now split by frontend edition.
# Use release-ant.yml, release-mui.yml, release-mantine.yml or release-shadcn.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This workflow is deprecated.'
          echo 'Use one of the edition-specific release workflows instead.'
